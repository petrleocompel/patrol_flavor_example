# patrol_flavor_example

Example of `patrol` flavor issue

## Getting Started

- run `patrol test --target integration_test/main_test.dart --verbose`
- device will not do anything
- `adb logcat | grep -i Patrol` is also empty
